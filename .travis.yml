language: java

jdk:
  - openjdk7
  - openjdk8

installation: true

jobs:
  include:
    # Run the tests
    - stage: test
      script: ./scripts/test-unit.sh
      script: ./scripts/test-regression.sh
      script: ./scripts/ant-build.sh
