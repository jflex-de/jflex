match: --a--
action [22] { yybegin(STATE2);
          return 0; }
match: --a--
action [22] { yybegin(STATE2);
          return 0; }
match: --a--
action [22] { yybegin(STATE2);
          return 0; }
match: <<EOF>>
action [39] { return 3; }
